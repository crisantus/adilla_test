import 'package:flutter/material.dart';

class Dilla extends StatelessWidget {
  const Dilla({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Dillia"),
      ),
    );
  }
}